# Milestone 01 To-Do

## Objective

Set up `rory-tradeR` as a secure, fork-derived trading research repo with the basic documentation and directory boundaries in place.

## Checklist

- [x] Merge or otherwise bring the upstream `prediction-market-analysis` codebase into this repo while preserving provenance.
- [x] Update the root `README.md` to explain the upstream relationship and new trading direction.
- [x] Create the initial `docs/trading/` document set.
- [x] Add repo structure for `src/exchanges/`, `src/trading/`, `data/`, and `runtime/`.
- [x] Add `.gitignore` rules for runtime state, local archives, env files, and extracted datasets that must not be committed.
- [x] Confirm `origin` and `upstream` remotes are correct.
- [x] Write down local repo boundary rules so this stays separate from the art app.
- [x] Record first-pass development conventions in `claude.md` and `AGENTS.md`.

## Exit Criteria

- [x] The repo clearly reads as a trading project, not an empty placeholder.
- [x] A new engineer can understand the repo purpose and upstream relationship from docs alone.
- [x] The repo has safe locations for source, docs, data, and runtime artifacts.
